# YOLO dataset config for this repository.
# Split paths are relative to this YAML file's parent directory.
train: ../dataset/images/train
val: ../dataset/images/val
test: ../dataset/images/test

names:
  0: plate
